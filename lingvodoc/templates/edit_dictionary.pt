<html>
<head metal:use-macro="load: head.pt">
</head>
<body>

<div metal:use-macro="load: nav.pt"></div>
<div class="container" style="padding-top: 20px">
    <div class="row">
        <div class="col-md-1">
            <button type="button" class="btn btn-default" data-bind="click: addNewMetaWord">
                <span class="glyphicon glyphicon-plus"></span>
            </button>
        </div>
        <div class="col-md-11 ">

            <table class="table table-striped">
                <thead>
                <tr>
                    <td class="col-md-4">Транскрипция</td>
                    <td class="col-md-4">Перевод</td>
                    <td class="col-md-3">Звук</td>
                    <td class="col-md-1"></td>
                </tr>
                </thead>


                <tbody>

                <!-- ko foreach: list -->
                <tr>
                    <td>
                        <div data-bind="foreach: transcriptions">
                            <span data-bind="text: content"></span>
                            <!-- ko if: $parent.addedByUser -->
                            <button type="button" class="btn btn-default btn-xs" data-bind="">
                                <span class="glyphicon glyphicon-remove-sign"></span>
                            </button>
                            <!-- /ko -->
                            <br>
                        </div>

                        <button type="button" class="btn btn-default btn-sm"
                                data-bind="click: $root.addValue.bind($data, $data, 'transcriptions')">
                            <span class="glyphicon glyphicon-plus"></span>
                        </button>

                        <br>
                        <!-- ko if: $root.isInputEnabled($data, 'transcriptions') -->
                        <input type="text" value="" placeholder="new value..."
                               data-bind="event: { blur: $root.saveValue.bind($data, 'transcriptions') }">
                        <!-- /ko  -->
                    </td>

                    <td style="display: inline-block">
                        <div data-bind="foreach: translations">
                            <span data-bind="text: content"></span>
                            <!-- ko if: $parent.addedByUser -->
                            <button type="button" class="btn btn-default btn-xs"
                                    data-bind="click: $root.removeValue.bind($data, 'translations')">
                                <span class="glyphicon glyphicon-remove-sign"></span>
                            </button>
                            <!-- /ko -->
                            <br>
                        </div>
                        <div>
                            <button type="button" class="btn btn-default btn-sm"
                                    data-bind="click: $root.addValue.bind($data, $data, 'translations')">
                                <span class="glyphicon glyphicon-plus"></span>
                            </button>
                        </div>
                        <div>
                            <!-- ko if: $root.isInputEnabled($data, 'translations') -->
                            <input type="text" value="" placeholder=""
                                   data-bind="event: { blur: $root.saveValue.bind($data, 'translations') }">
                            <!-- /ko  -->
                        </div>
                    </td>

                    <td style="display: inline-block">
                        <div data-bind="foreach: sounds">
                            <span data-bind="text: content"></span>
                            <!-- ko if: $parent.addedByUser -->
                            <button type="button" class="btn btn-default btn-xs"
                                    data-bind="click: $root.removeValue.bind($data, 'sounds')">
                                <span class="glyphicon glyphicon-remove-sign"></span>
                            </button>
                            <!-- /ko -->
                            <br>
                        </div>

                        <!-- drag and drop area for upload -->
                        <div>
                            <div class="drag-n-drop" style="border: 1px dashed #ccc; width: 100%; height: 80px;" data-bind="dragndropUpload: $root.saveSound.bind($data, $data)"></div>
                        </div>
                    </td>


                    <td data-bind="if: $data.addedByUser">
                        <button type="button" class="btn btn-default btn-sm" data-bind="click: $root.removeWord">
                            <span class="glyphicon glyphicon-remove-sign"></span>
                        </button>
                    </td>

                </tr>

                <!-- /ko -->

                </tbody>

            </table>
        </div>
    </div>
</div>
<script data-main="${request.static_url('lingvodoc:static/js/lingvodoc.js')}" src="${request.static_url('lingvodoc:static/js/require.js')}"></script>

</body>
</html>
